import { SectionTitle } from '@/components/ui/SectionTitle.jsx'

export function CheckoutPage() {
  return (
    <div className="tn-container tn-section-y">
      <SectionTitle
        eyebrow="Secure"
        title="Checkout"
        subtitle="Stripe / Razorpay integration lands here after cart persistence ships."
      />
    </div>
  )
}
