import { SectionTitle } from '@/components/ui/SectionTitle.jsx'

export function WishlistPage() {
  return (
    <div className="tn-container tn-section-y">
      <SectionTitle
        eyebrow="Saved"
        title="Wishlist"
        subtitle="Protected route — wire to MongoDB wishlist collection in the next commerce phase."
      />
    </div>
  )
}
